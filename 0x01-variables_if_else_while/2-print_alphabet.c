#include <staio.h>
/**
 * main - sometimes suffer from insomnia
 * Return: Always 0(Succecc)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
